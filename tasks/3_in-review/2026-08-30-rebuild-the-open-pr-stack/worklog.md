# Worklog — recover the open PR stack

Append-only; newest at the bottom. One entry per session that touched this task.

## 2026-08-30 — rebuild-the-open-pr-stack (codex)

- The owner authorized autonomous research and recovery of PRs 88 and 89, including replacement PRs if needed. The existing checkout and unfinished merge remain preserved.
- Three isolated research units assess PR 88, PR 89, and local leftovers. The orchestration run records the exact request and source backups outside the tracked tree.

## 2026-08-30 — research and design (codex)

- The complete owner index matches probe commit 63abfe0; no independent useful local behavior was found. Original refs and staged merge metadata are preserved.
- PR 88 has four conflict paths, two generated. Its existing AST compatibility fix resolves the Python 3.14 baseline failure. The new raw skeleton check incorrectly refuses ordinary retry diagnosis notes.
- PR 89 has verified successor-boundary, answered-successor, source-less form, missing-source, angle-link, and unstaged-symlink defects. Design attack additionally identified identifier loss during quote normalization and the need to preserve target/context lineage.
- Direct-main coordination publication was rejected by auto-review. Filing and claim commits were published on this recovery branch; origin/main is unchanged. The recorded coordination-only baseline passes all 16 test files under system Python 3.9; main fails the AST helper under Python 3.14, which the retained PR 88 already repairs.

## 2026-08-30 — lower repairs and stack admission (codex)

- Preserved the original lower history on current main. The runtime milestone passed all 16 test files on Python 3.14 and 3.9, plus three Git-bearing corpus checks in a true clone. Eleven retry regression methods passed; damaged controls were observed failing. Exact outputs will be recorded in verification before review.
- A genuine copy/fill test, retaining real fold tags and exercising human-attention and fold-shape checks, failed on all three human templates because of their instruction comments. Moved those instructions into the canonical guide; the improved test and strict HTML negative control now pass. The normal commit hook passed its one selected test file; 15 unrelated files were not selected.
- A normal push update of the old upper PR rechecks historical transcript edges already on main. The replacement child branch starts at the lower layer, preserves the original upper commits, and passed the normal merge hook and full-OID admission checks. No hook or historical record was weakened.

## 2026-08-31 — independent lower refutations and repaired source exposure (codex)

- Independent review reproduced reversed fold tags, nested/multiline reference definitions, hidden mutable fields, and visible Unicode falsely treated as invisible. The parent repaired fold ordering and retry reference/Unicode handling; all new regressions were observed red and green through the normal hooks.
- The runtime worker added an exact local exposure view and a narrowly byte-preserving first-human-response comparison. Its final lower tip f41bb8eab9137b84bd4eeba990ba72336d3adbd4 passed all 16 test files on Python 3.14 and 3.9, with 17 new focused methods and explicit damaged controls. The shared Markdown parser was not changed by that field repair.
- Parent and independent design review identified escaped and incomplete entity spellings that are visible CommonMark text. A bounded Boolean detector preserves those literal examples while protecting actual invisible references, including when opening backticks are escaped. It decodes complete references once and leaves original source bytes untouched.
- Promoted the exact owner request and source-disposition inventory, including all 79 original staged records. Removed an unverified authoring-trial statistic from active word-count guidance; the budget and command are unchanged. Final combined review, cold-clone checks, and PR publication remain pending.

## 2026-08-31 — portable format-control classification (codex)

- Final independent review reproduced a Python-version split: raw and encoded Egyptian format controls were editable on Python 3.9 but frozen on 3.14. The new Boolean detector now uses the complete Unicode 16 format-control ranges, checked against the published Unicode data, instead of the interpreter’s bundled category database. The older prose normalizer remains unchanged.
- Two regression methods exercise Arabic and Egyptian format controls in real Git fixtures. They include raw and encoded spellings in retry diagnosis and human-item metadata, plus visible neighboring characters. The Python 3.9 baseline produced 24 failures across 2 methods. Both repaired interpreters passed those 2 methods without skips. The parent still owns the final combined verification.

## 2026-08-31 — verified replacement publication and closing records (codex)

- Five independent native lenses accepted the lower code revision. Parent worktree and full-history cold-clone runs passed all 16 test files on Python 3.14 and 3.9; the three true-Git corpus checks passed separately with no skips. `verification.md` preserves the actual commands and output, and `regression-evidence.json` preserves per-method failing controls.
- Both replacement code candidates were published and GitHub reported them mergeable with clean checks before the original PRs were closed. The stack remains open, with the upper branch based on this lower task branch. Closing record changes still pass through ordinary hooks and refreshed hosted checks.
- The original owner branch, 79 staged records, 718-entry index, unfinished merge metadata, probes and backups remain intact. No independent implementation was imported from discarded local experiments.
- The separate external review did not run: execution security refused transmission before Claude started. Its canonical question withholds only that external operation. The plan records this limit rather than claiming an unavailable cross-vendor result.
