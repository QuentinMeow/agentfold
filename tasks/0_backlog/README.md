# 0_backlog/ — filed, not started

Unclaimed task folders. Anyone may file here by copying `templates/task/`; each task's
linked non-blocking `message-queue/needs-agent/requests/` item with
`Request kind: task-pickup` is the canonical pickup
action. Claiming the task resolves that request. Lifecycle: `tasks/AGENTS.md`.
