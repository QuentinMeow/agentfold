# Verification — mine markdown co-change couplings and validate heading anchors

**Verified:** nothing yet — the task was filed on 2026-07-25 by claude and no plan step has run

Only commands actually run and their real output — never expected or paraphrased
output (root `AGENTS.md` guardrail). A reader must be able to re-run every line.

This file is empty of results on purpose. Each step in `plan.md` appends its own section
below, holding the exact command that was run and the real output it printed, pasted rather
than summarised. Sections that belong here when the task reaches review:

```
## before-state: the anchor hole
## link-check: missing path behind a fragment
## link-check: unknown fragment in an existing file
## mining report on this repository
## ledger: a rejected pair does not re-surface
## rejection rate against the governance bands
## unit tests
## reconciler
## the gating experiment over two hot files
```

Nothing above has been run, so nothing is recorded. The warm-up limitation stated in
`design.md` applies to every number that lands in the mining sections.
