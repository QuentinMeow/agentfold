# Plan — Codify queue-message disposability and README depth rules

- [x] 1. Add the "projections, not sources" rule to `message-queue/AGENTS.md`
- [x] 2. Add the disposable-context guidance to `handbook/decision-guide.md`
- [x] 3. Extend the root `AGENTS.md` README line with the depth rule
- [x] 4. Add a root-README line budget to `reconcile.py`; note it in the README's
      enforcement table
- [x] 5. Verify: reconciler + tests green; budget check demonstrably fires on an
      over-budget README
- [x] 6. Record ADRs for both rules; regenerate the memory index; update
      `roadmap/current-state.md`
