# 4_done/ — merged and verified

Finished tasks, kept ~90 days as worked examples, then pruned by the memory gardener
(learnings promoted to `memory/` first; git history archives the rest).
Lifecycle: `tasks/AGENTS.md`.
