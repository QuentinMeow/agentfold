# Worklog — preserve evidence and unanswered obligations

## 2026-08-30 — open-PR stack recovery (codex)

- The owner authorized replacement PRs and all useful final changes. This bounded child task owns the upper source-evidence repairs; the parent owns the overall stack and preserved local data.
- Filed and claimed from the recovered lower branch, which already contains current main. Direct-main coordination publication was rejected by auto-review earlier in this run, so coordination is published on the task branch before implementation.
- Importing current main into the old upper head replays two already-main historical transcripts under a withdrawn grammar. Independent scratch verification shows a lower-first merge retains identical bytes and all original PR89 commits while its trusted-base range passes. An ordinary push of that merge to the old upper branch still replays those records; a new claimed upper task starts from the valid current-main baseline without changing a gate.

## 2026-08-30 — runtime and independent source checks (codex)

- The worker committed captured-source and exact-successor repairs, then preserved literal string spacing and partial emphasis. Both Python environments passed all 16 test files; 20 new methods have observed-red controls and appear in the full runner. A true clone ran all three Git-bearing corpus checks. These milestone results precede the final lower-layer integration.
- Independent documentation and coverage review found valid root-relative line citations blocked by the generic link scan, root/local and unstaged-symlink disagreement between checks, and Unicode separators manufacturing nonexistent source selections. Four new actual-template methods fail against the preceding runtime with 54 failing subcases and pass after the parent repair; normal hooks passed 3/3 selected test files.
- The source guide now names hash-style headings and literal-preserving presentation. Its teaching quotation is a complete exact sentence from the linked source. Eighteen selected source/template methods passed, and the normal guide commit hook passed its one selected test file. Unsupported underlined-heading anchors remain reported; bounded line selection is the documented alternative.
- Removed unsupported quantitative and provider-popularity rhetoric from implementation comments. No parser behavior changes in that cleanup. The original historical records remain intact; final combined reviews, exact ranges, and hosted CI are still pending.

## 2026-08-31 — final quotation counterexamples and lower integration (codex)

- Independent review demonstrated truncated identifier/number matches, normalized inline-code spaces, and omission markers acting inside string literals. The runtime worker repaired token boundaries and preserved literal spans through the permitted presentation changes. Its committed tip is 284006004247949728904c68ea7af3d245017c71.
- Six added methods failed 48 subcases against the prior runtime. The focused suite passed 24/24; both full local Python suites passed all 16 files. Fresh Git-bearing corpus runs passed 3/3 on each interpreter. Additional damaged controls covered first-occurrence matching, presentation, and omissions.
- The parent combined the reviewed lower Unicode repair from 5a9f044f84fbcc21597f7bf44466cab9b3694b42 by a normal merge. Final combined native review, exact candidate checks, and upper PR publication remain parent-owned.
