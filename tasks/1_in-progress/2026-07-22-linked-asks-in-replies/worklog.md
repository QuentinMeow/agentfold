# Worklog — Surface needs-human items as clickable links plus context

Append-only; newest at the bottom. One entry per session that touched this task.

## 2026-07-22 — doc-depth-invariants session, continued (claude, chat session)

- Filed and claimed from a chat rule in the same conversation as
  2026-07-22-doc-depth-invariants; audited first: template, ritual step 4, and the
  skill all said "with links" but none pinned clickable-link-plus-context, so a bare
  "pending decision: ABC" passed.
- Chose `templates/handover.md` as the format's single source; the other two texts
  reference it (single-source-of-truth guardrail).
