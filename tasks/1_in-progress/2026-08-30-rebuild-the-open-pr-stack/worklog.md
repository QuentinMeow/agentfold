# Worklog — recover the open PR stack

Append-only; newest at the bottom. One entry per session that touched this task.

## 2026-08-30 — rebuild-the-open-pr-stack (codex)

- The owner authorized autonomous research and recovery of PRs 88 and 89, including replacement PRs if needed. The existing checkout and unfinished merge remain preserved.
- Three isolated research units assess PR 88, PR 89, and local leftovers. The orchestration run records the exact request and source backups outside the tracked tree.

## 2026-08-30 — research and design (codex)

- The complete owner index matches probe commit 63abfe0; no independent useful local behavior was found. Original refs and staged merge metadata are preserved.
- PR 88 has four conflict paths, two generated. Its existing AST compatibility fix resolves the Python 3.14 baseline failure. The new raw skeleton check incorrectly refuses ordinary retry diagnosis notes.
- PR 89 has verified successor-boundary, answered-successor, source-less form, missing-source, angle-link, and unstaged-symlink defects. Design attack additionally identified identifier loss during quote normalization and the need to preserve target/context lineage.
- Direct-main coordination publication was rejected by auto-review. Filing and claim commits were published on this recovery branch; origin/main is unchanged. The recorded coordination-only baseline passes all 16 test files under system Python 3.9; main fails the AST helper under Python 3.14, which the retained PR 88 already repairs.

## 2026-08-30 — lower repairs and stack admission (codex)

- Preserved the original lower history on current main. The runtime milestone passed all 16 test files on Python 3.14 and 3.9, plus three Git-bearing corpus checks in a true clone. Eleven retry regression methods passed; damaged controls were observed failing. Exact outputs will be recorded in verification before review.
- A genuine copy/fill test, retaining real fold tags and exercising human-attention and fold-shape checks, failed on all three human templates because of their instruction comments. Moved those instructions into the canonical guide; the improved test and strict HTML negative control now pass. The normal commit hook passed its one selected test file; 15 unrelated files were not selected.
- A normal push update of the old upper PR rechecks historical transcript edges already on main. The replacement child branch starts at the lower layer, preserves the original upper commits, and passed the normal merge hook and full-OID admission checks. No hook or historical record was weakened.
