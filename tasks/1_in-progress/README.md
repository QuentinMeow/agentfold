# 1_in-progress/ — claimed and moving

Every task here has a `**Claimed-by:**` in its `task.md` and a `plan.md`. A task
untouched for 14+ days gets flagged by the reconciler — reclaim it or move it back to
`0_backlog/`. Lifecycle: `tasks/AGENTS.md`.
