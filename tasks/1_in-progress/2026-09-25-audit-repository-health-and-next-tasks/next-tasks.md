# Next-task sequence — Repository health and workflow proof

This is an agent-derived execution recommendation within the owner's existing priority
order. The portable multi-agent collaboration goal remains first; the web task viewer and
one-command adoption remain deferred. This document links canonical work rather than
creating another action ledger.

The current maintenance pass changes descriptive records only. It does not implement
continuity repair, publication authority, takeover, or the five acceptance experiments.

## Recommended order and evidence boundary

### 1a. Inherited queue mutations

A restack can combine an already committed answer and claim into a synthetic mutation. The first check is a red regression against the current checker; then real-edge validation must preserve refusal of actual rewrites.

[When this backlog item is selected, claim it and remove this completed pickup request in the same coordination commit.](message-queue/needs-agent/requests/non-blocking-pick-up-judge-inherited-queue-mutations-on-their-real-edges.md)

### 1b. Claim continuity across an absence

The existing merge-claim task owns the opposite safety control: an unrelated historical
claim cannot authorize an action that disappeared and was introduced again. It remains a
separate change with its own observed-red regression; continuity repairs must not make
this unsafe case pass.

[When this backlog item is selected, claim it and remove this completed pickup request in the same coordination commit.](message-queue/needs-agent/requests/non-blocking-pick-up-stop-a-merge-from-borrowing-a-claim-across-an-absence.md)

### 2. Expected-tip publication

The publisher records the exact remote object ID before rewriting and refuses stale publication even after a sibling fetch. The existing task already has the disposable race and acceptance criteria.

[Claim the explicit-lease task and remove this pickup request in the same coordination commit.](message-queue/needs-agent/requests/non-blocking-pick-up-bind-task-branch-pushes-to-observed-tips.md)

### 3. Coordination and writer handoff

The corrected backlog interpretation requires positive stop or revocation evidence. A claim race with an unrelated concurrent update must preserve one owner and both changes; the implementation also needs a supported publication path when direct-main writes are unavailable.

[When this backlog item is selected, claim it and remove this completed pickup request in the same coordination commit.](message-queue/needs-agent/requests/non-blocking-pick-up-finalize-coordination-write-rules.md)

### 4a. Imported-history attribution

The existing staged-merge reproduction compares identical trees with different contexts. Its repair remains serialized with the continuity checker and must still report changes actually authored by the receiving branch.

[Repair the staged-merge reconciliation path so a task branch is not blocked by findings in unchanged history imported from main, while preserving findings for defects the branch authored.](message-queue/needs-agent/requests/non-blocking-stop-pre-commit-from-re-auditing-imported-history.md)

### 4b. Durable handover links

Status moves currently invalidate historical task-folder links. This task already owns the stable-reference problem; the audit preserves immutable handovers instead of rewriting them.

[When this backlog item is selected, claim it and remove this completed pickup request in the same coordination commit.](message-queue/needs-agent/requests/non-blocking-pick-up-make-a-handover-survive-the-next-commit.md)

### 4c. Task-status drift detection

Seven manual closeouts in this audit show the recurring maintenance cost. The existing task must distinguish a merged PR from complete acceptance and keep provider queries off ordinary commit checks.

[When this backlog item is selected, claim it and remove this completed pickup request in the same coordination commit.](message-queue/needs-agent/requests/non-blocking-pick-up-notice-a-task-whose-work-already-merged.md)

### 5. Five observed workflow scenarios

The new bounded child task produces operational evidence and a short manual after the prerequisites it relies on are ready. A designed rule or a passing unit suite is not a passing end-to-end scenario.

[Claim the five-scenario workflow proof task and record observed results plus its operations manual after its prerequisite repairs are ready.](message-queue/needs-agent/requests/non-blocking-pick-up-five-parallel-workflow-scenarios.md)

## Dependency and concurrency limits

Steps 1, 3 and 4a can touch the same reconciliation and Git-boundary code, so their
implementations are serialized or explicitly stacked on reviewed commits. The publication
helper may be developed separately only after its exact file ownership is known. Durable
record work can proceed independently when it does not change those boundaries.

The stale-base task remains in review: its recorded acceptance is not fully checked off,
and its old prescribed merge order was not observed. The parent multi-worktree task remains
in progress because its runtime proof is unfinished. The first-class queue task retains
its live repair. None is marked complete merely because a PR merged.

The six baseline readability advisories describe frozen human questions. Their canonical
replacement/disposition actions remain the authority; this plan does not authorize an
agent to rewrite them. Existing start gates on guard configuration and the unexecuted
external-review operation retain their boundaries.

## Verification expectations for the next implementation

Every checker repair has a specific observed-red reproduction, a green repaired run and
negative controls that retain the rejected unsafe behavior. Workflow evidence names the
exact environment and distinguishes local simulated loss from actual machine recovery.
The five-scenario task must demonstrate both combined-failure detection and non-advancement
of the observed landing target, or record that protection as unverified.
