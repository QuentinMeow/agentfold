# Plan — collapse restated, circular, and unenforced contract rules

- [ ] Break the precedence loop: delete the inverted clause in `handbook/AGENTS.md` and
      point root `AGENTS.md` at the one principle that owns precedence
- [ ] Delete the delivery-prefix restatement from the five queue templates, replacing it
      with one link to `message-queue/AGENTS.md`
- [ ] Delete it from the eight remaining live contracts (`templates/README.md`, three
      handbook guides, four skills, the reviews README), folding in first anything a copy
      carried that the owner does not
- [ ] Make the retry-filing, guard-mode, branch-lane, mirror-sketch, and link-check
      claims conditional or delete them; file the principles-file one as a human decision
      because `handbook/principles/` is near-immutable
- [ ] Add `**Amends:**`/`**Amended-by:**` to `templates/memory/adr.md` and the rule to
      `memory/AGENTS.md`
- [ ] Write the missing lineage metadata on both broken ADR chains without touching any
      decided ADR's prose
- [ ] Teach `generated_index()` to mark an amended ADR, then regenerate `memory/index.md`
- [ ] Re-read every deleted rule from a natural starting point by following links
- [ ] Record real `--check` and `run_tests.py` output in `verification.md`
