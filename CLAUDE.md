@AGENTS.md

<!-- This file exists because some tools auto-load CLAUDE.md, not AGENTS.md.
     AGENTS.md is the canonical agent contract — edit that file, never this one.
     automation/install.py creates git-ignored CLAUDE.md -> AGENTS.md symlinks in
     subfolders; the root uses this import file so Windows checkouts work. -->
