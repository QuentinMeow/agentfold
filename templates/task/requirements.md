# Requirements — <task title>

Owner words only, verbatim and dated: an entry is appended and never edited, reordered, or
deleted. Interpretation lives in `task.md`, where every acceptance criterion says whether
the owner or an agent supplied it.

## <YYYY-MM-DD> — <chat | answer to `<queue item path>` | task `<parent id>` | goal G<n> | <provider url>>

```text
<the owner's words, exactly as written — every character, however long>
```

<!-- A fence rather than a blockquote, on purpose: the reconciler's unqueued-ask scanner
reads a visible blockquote as actionable prose and fenced code as data, and a fence keeps
the bytes exact. A task with no owner words at all carries no dated entry and no fence;
in their place it keeps this single line, filled in:
No owner words — filed by <who> from `<durable source path>`. -->
