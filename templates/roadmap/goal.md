## G<n> — <the goal, one line, specific enough to spawn tasks against>

**Asked:** <YYYY-MM-DD>, by <the owner | agent <who>>, from <chat | answer to `<queue path>` | `<design or handover path>`>
**Confirmed:** <YYYY-MM-DD by owner | no — agent-proposed, clarification `<needs-human path>`>

```text
<the owner's words that state this goal, exactly — or, for an agent-proposed goal, the one sentence the agent asks the owner to confirm>
```

<Two or three sentences: what done looks like, then the current status in italics. A
retired goal keeps its heading and replaces `Confirmed:` with `**Retired:** <YYYY-MM-DD>
— `<decision path>``, so an old task's Fit line still resolves.>
